apiVersion: v1 
kind: Service
metadata:
  name: my-service
spec:
  type: NodePort
  selector:
    app: myapp
    replica: blue
  ports:
  - port: 80 
    targetPort: 80
    protocol: TCP
    nodePort: 30007

