apiVersion: v1
kind: Pod
metadata:
  name: sample-pod
spec:
  serviceAccountName: my-service-account
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80
